{
  "name": "@agent-guardrails/contracts",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@agent-guardrails/domain": "workspace:*",
    "zod": "catalog:core"
  },
  "devDependencies": {
    "typescript": "catalog:core",
    "vitest": "catalog:test"
  }
}
