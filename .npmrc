engine-strict=true
strict-peer-dependencies=true
auto-install-peers=false
resolution-mode=highest

# eslint-plugin-sonarjs requires `globals` at runtime without declaring it; hoist so nested resolution finds it.
public-hoist-pattern[]=globals
