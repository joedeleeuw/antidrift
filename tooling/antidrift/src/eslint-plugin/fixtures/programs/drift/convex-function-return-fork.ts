// Hand-written exact structural copy of FunctionReturnType<typeof api.machines.get>
// (the MachineSummary return type owned by convex/_generated/api). Expected: 1 error.
export type LocalMachineSummary = {
  id: string;
  name: string;
  address: string;
  online: boolean;
};
