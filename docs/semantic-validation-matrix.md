# Semantic Validation Matrix

This matrix turns the semantic drift goal into an execution checklist. Use it before adding, promoting, retiring, or migrating an `antidrift/*` rule.

The rule name is not the proof. A rule blocks only when its carrier can prove the broken association it claims to own. When the carrier cannot prove that association, the rule stays inventory-only, research, delegated to ecosystem tooling, or retired.

## How To Use This Matrix

Every active custom rule and serious candidate needs a row before promotion. The row must answer:

- What proof object justifies the finding?
- Which carrier owns the proof: plain ESLint, type-aware ESLint, semantic adapter, authority registry, repo graph, agent-ops, or model-assisted discovery?
- What exact threshold turns evidence into blocking behavior?
- Which real corpus files prove drift and clean controls?
- What happens when the semantic fact sink, registry, parser services, or corpus evidence is missing?

Reduced fixtures are regression locks only. Real codebase copies are the promotion gate for drift, clean controls, owner discovery, and false-positive pressure.

## Semantic Or Authority Rules To Bring Over

These rows should use shared semantic adapters, TypeScript parser services, registry facts, or emitted semantic facts. "Bring over" means the intent survives under a stronger proof carrier; it does not mean every existing branch keeps blocking severity.

| Rule or candidate                                  | Carrier                                                 | Semantic association or authority fact                                                                                                                          | Blocking threshold                                                                                                                                                                                                                                                                                                                                                           | Validation and gap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | No-sink or no-dead-work behavior                                                                                                                                                                                                                          |
| -------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `antidrift/no-handrolled-resource-lifecycle-cells` | React state graph semantic adapter                      | React state setter to cell; state cell to resource lifecycle role; handler frame, async/request guard, lifecycle constant, sibling resource write               | Block only deterministic lifecycle-cell redundancy: a transition toggles a boolean lifecycle cell, writes a distinct error cell inside catch, and assigns a distinct payload cell from an awaited value or first-level awaited source member. Request-guarded transitions, updater payloads, and broad multi-setter updates stay inventory-only.                              | `pnpm policy:inventory-react-state` classifies broad co-mutation separately from diagnostics. Fixed-proof scans checked 3,323 files across Chaski, Sudocode, Murderbox, Codebase Atlas, Opencode UI/console, Cloudflare Agents, and PowerSync; they emitted 9 diagnostics across Chaski, Sudocode, and Cloudflare Agents while Murderbox, Codebase Atlas, Opencode, and PowerSync stayed diagnostic-clean. Promotion still needs human review of positives plus pagination/request-guard/form/mutation clean controls. | Without a fact sink, deterministic lifecycle diagnostics still report; broad co-mutation remains inventory-only and never blocks.                                                                                                                         |
| `antidrift/no-shattered-ingested-entity-state`     | React state graph semantic adapter                      | React state setter to cell; source object member to state cell                                                                                                  | Inventory-only — records (no diagnostic) when one transition assigns at least two distinct members of the same freshly awaited source object into at least two sibling cells, after excluding controlled input draft cells with event-derived edits. The owned-entity enforcement tier was removed after multi-repo scans found no real human-authored owned-entity shatter. | RuleTester covers source-member fan-out and envelope/controlled-input discrimination — the rule emits an inventory candidate but never a diagnostic. A multi-repo scan (chaski, sudocode) found zero real owned-entity shatters; `src/frontend/monolithui/src/components/EditPOModal.tsx` is kept as a controlled-input clean control. Re-promotion to enforcement needs real owned-entity shatters mined across repos (likely an agent-generated corpus) plus clean controls for pagination metadata, SWR/query resources, and intentional denormalized view state. | Without a fact sink, no `sourceMemberStateShardCandidate` is recorded; the rule never produces a diagnostic either way.                                                                                                                                   |
| `antidrift/no-status-triplet-state`                | Retired; replaced by React state graph                  | Configured name groups are only historical inventory hints; proof belongs to semantic AST setter-to-cell lifecycle association                                  | No standalone blocking. The lifecycle-cell subset is implemented in `no-handrolled-resource-lifecycle-cells`                                                                                                                                                                                                                                                                 | Locked retired decision. Reopen only with new real-code evidence and a deterministic proof that is not already covered by `no-handrolled-resource-lifecycle-cells`.                                                                                                                                                                                                                                                                                                                                                                                                  | No standalone rule, sink, or blocking enforcement remains; name groups are historical inventory context only.                                                                                                                                             |
| `antidrift/no-structural-type-fork`                | TypeChecker plus authority index and semantic fact sink | exact local structural type copy to generated owner; exact local structural type copy to domain owner; exact local structural type copy to accepted package owner | Block exact generated/domain/accepted package owner copies only. Installed-package similarity without accepted owner authority emits proposal facts when a sink exists; projected subsets stay clean.                                                                                                                                                                          | Exact-copy fixtures validate blocking behavior; subset/projection fixtures and unaccepted package-copy fixtures validate inventory/no-sink behavior. Stable still needs independent generated-source replication and real package-owner evidence before adding accepted package owners.                                                                                                                                                                                                              | Accepted owner diagnostics still report without a sink; unaccepted installed-package owner proposals are dropped without a sink.                                                                                                                          |
| `antidrift/no-canonical-model-fork`                | TypeChecker plus authority index and domain registry    | local structural type to domain owner; local object contract to configured canonical entity owner                                                               | Block only configured owner forks above the structural threshold                                                                                                                                                                                                                                                                                                             | Chaski and Sudocode now provide configured-owner drift evidence. Stable promotion still needs adversarial review and broader boundary DTO/view-model clean pressure; schema-inferred contracts stay clean.                                                                                                                                                                                                                                                                                                                                                           | Missing domain owner config means no authority claim; configured-owner diagnostics do not depend on fact collection.                                                                                                                                      |
| `antidrift/no-status-literal-in-type`              | authority index plus AST owner-context proof        | status literal to configured domain status owner by exact context name or enclosing object/property composition                                                                                 | Block only configured domain status owners outside owner files when the context resolves to the owner name (`RepProgressStatus` or `RepProgress.status`)                                                                                                                                                                                                                                                                                                               | Needs independent status-fork replication. Generic UI variant unions, unrelated local status props, and bare `status` variables with overlapping values must stay clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Missing status owner config means no authority claim; owner files, generic UI variants, unrelated status unions, and bare status variables stay clean.                                                                                                                                                     |
| `antidrift/no-redundant-zod-parse`                 | TypeChecker plus schema provenance                      | Values already proven as a Zod schema output to later parse calls on the same schema.; parsed value to schema provenance                                        | Same-schema reparse reports only when TypeScript proves the consumed value is already equivalent to that schema output.                                                                                                                                                                                                                                                      | Stable path remains type-aware ESLint. Future same-schema recertification with object spread is separate research.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | This rule emits no semantic facts; diagnostics depend on parser services and local provenance, not sink availability. First boundary parses, different storage schemas, and assertion-callback contract checks stay clean.                                |
| `antidrift/no-sql-string-concat`                   | SQL context plus local dataflow and TypeChecker proof   | SQL string fragments to bound values, identifier allowlists, escaping helpers, import-bound builders, declaration-source member builders, safe members, or unsafe interpolation.; SQL fragment to binding proof            | Unsafe value interpolation in SQL context reports; identifier interpolation reports only when the checker can prove a bound, allowlisted, escaped, static, configured-safe source, import-bound builder, or declaration-source member builder. SQL-builder tag/member APIs are not clean by name.                                                                                                                                       | `pnpm policy:benchmark-sql-queries` emits `parserServiceDeltas`: extra-only non-type-aware identifier reports are conservative inventory; missing non-type-aware findings, parser errors, or new unclassified SQL ecosystems block promotion. SQL-builder/tagged-template clean controls must move from known-gap to import/declaration-source proof before blocking promotion.                                                                                                                                                                                                  | This rule emits no semantic facts; benchmark and inventory tooling read diagnostics and parser-service deltas directly. Placeholder lists, static fragments, typed unions, regex exit guards, quote-doubling escapers, import-bound builders, and declaration-source member builders stay clean; unproven SQL tag/member APIs remain known gaps. |
| `antidrift/no-underchecked-type-predicate`         | TypeChecker plus AST/control-flow checks                | broad input to asserted object contract                                                                                                                         | Default-off until real required-field drift is found. Optional-field sufficiency stays inventory/research unless a stronger proof exists                                                                                                                                                                                                                                     | After required-property narrowing, `pnpm policy:inventory-underchecked-predicate` checks 1,353 files, 64 candidate files, 112 predicate signatures, and finds 0 blocking diagnostics with 0 parser errors.                                                                                                                                                                                                                                                                                                                                                           | Default-off inventory stays non-blocking; missing type services or required-field proof produces no diagnostic.                                                                                                                                           |
| `antidrift/no-appeasement-cast`                    | TypeChecker plus AST/control-flow checks                | broad any/unknown source to named object contract assertion                                                                                                     | Block broad source into trusted named target. Typed-source conversions and SDK-owned conversions stay clean                                                                                                                                                                                                                                                                  | Use upstream `@typescript-eslint/no-unsafe-type-assertion` as broad benchmark, not replacement, unless consumer policy accepts the larger surface.                                                                                                                                                                                                                                                                                                                                                                                                                   | Missing type services or broad-source proof leaves the assertion to upstream unsafe-type rules instead of custom blocking.                                                                                                                                |
| `antidrift/no-unsafe-deserialize`                  | TypeChecker plus local parse-input control flow         | JSON.parse input provenance to broad any/unknown values and local string-boundary proof                                                                          | Broad any/unknown parse input reports unless TypeScript or local control flow proves the parse input is a string.                                                                                                                                                                                                                                                            | Parse-output assertions belong to `no-appeasement-cast`. Clean parse-at-edge pipelines stay clean when the parse input is already string-typed or locally narrowed before schema validation.                                                                                                                                                                                                                                                                                                             | This rule emits no semantic facts; diagnostics depend on TypeScript parser services and local parse-input provenance. Already-string inputs, string-typed inputs that are immediately schema-validated, and parse-output cast cases owned by no-appeasement-cast stay clean. |
| `antidrift/no-async-array-method`                  | AST callback shape plus optional local scope binding     | async array callback to never-await method semantics; opt-in map or flatMap promise collection to later Promise combinator consumption                          | Default diagnostics cover never-await methods by AST contract. `map` and `flatMap` require explicit additive `branches: ["requires-collection"]` and local proof that the promise collection is neither returned nor joined by a Promise combinator                                                                                                                               | Validate `forEach(async ...)` drift separately from `Promise.all`, `Promise.allSettled`, assigned arrays, returned arrays, and cleanup loops.                                                                                                                                                                                                                                                                                                                                                                                                                         | Pending `map`/`flatMap` records exist only when the opt-in branch is configured; records are cleared by return or Promise combinators, and unresolved records report at function exit.                                                                     |
| `antidrift/require-authz-check`                    | handler-local request-param shape plus explicit authz function config | route params to nearby caller-configured authz call inventory                                                                                                   | Current absence-of-call rule stays default-off. Durable blocking should prefer typed policy-wrapper route registration or delegated dataflow/dominance proof                                                                                                                                                                                                                 | Validate middleware and wrapper clean controls before enabling beyond inventory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Missing route/authz registry facts, middleware dominance, and wrapper-owned authorization keep the rule scoped/off instead of global absence-of-call bans.                                                                                                |
| `antidrift/no-defensive-shape-probing`             | TypeChecker plus AST/control-flow checks                | broad Object.entries value to mini-parser probe cluster                                                                                                         | Default off package inventory. Do not promote unless new real `unknown`-typed drift proves value beyond upstream unsafe-member rules and `no-underchecked-type-predicate`                                                                                                                                                                                                    | `pnpm policy:inventory-defensive-shape` is parser-clean and currently finds no second drift across 1,680 type-aware files and 72 syntax candidates.                                                                                                                                                                                                                                                                                                                                                                                                                  | Default-off inventory remains non-blocking; without second drift or broad-value proof, syntax candidates are classified only.                                                                                                                             |

## Plain Source Rules To Keep Non-Semantic

These are still legitimate antidrift rules when the syntax or local string is the policy surface. They should use `ESLintUtils.RuleCreator`, RuleTester, and real corpus controls, but they do not need semantic fact emission unless a future branch adds semantic proof.

| Rule                                              | Carrier                                           | Semantic association or authority fact                                                                             | Blocking threshold                                                                                                                                      | Validation and gap                                                                                                                 | No-sink or no-dead-work behavior                                                                                                                                                                                   |
| ------------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `antidrift/require-effect-deps`                   | Import binding plus AST                           | React effect hook import/member bindings to calls missing a dependency array argument.                             | A React useEffect/useLayoutEffect call reports when no dependency array argument is provided.                                                           | RuleTester plus Chaski and Claude Code Source drift; real every-render disables with required descriptions stay clean              | This rule emits no semantic facts; diagnostics are independent of sink availability. Calls with dependency arrays, non-React useEffect bindings, and policy-described every-render disables stay clean.            |
| `antidrift/no-trivial-selector-wrapper`           | AST structural return shape                       | Explicitly annotated helper return types to direct property access on that helper's own parameter.                 | A wrapper reports only when the function body is just the selected property and the annotation restates what inference already knows.                   | Chaski, Codebase Atlas, and Murderbox drift; real transforming helpers, callback adapters, and inferred-return controls stay clean | This rule emits no semantic facts; diagnostics are independent of sink availability. Nontrivial computations and adapter callbacks without redundant explicit annotations stay clean.                              |
| `antidrift/no-inline-structural-type-at-use-site` | AST boundary context plus JSX-return clean exclusion | Anonymous object type literal to exported non-JSX, exported-class-method, or boundary-object surface contract      | Default-off inventory: report inline object contracts at exported non-JSX functions, exported-class methods, and boundary object methods; JSX-backed props, callback payloads, and small internal shapes stay clean | Chaski drift exists; independent repo replication is still required before blocking severity                                  | No semantic sink is involved; local/internal-only shapes are intentionally clean.                                                                                                                                  |
| `antidrift/no-nullable-positional-tuple`          | AST tuple shape plus TypeChecker alias resolution | tuple element positions to optional or nullish slot proof; tuple element aliases to TypeChecker nullish resolution | Default-off inventory: report tuples with at least two nullable or optional slots; one nullable hook-style slot, plain coordinate tuples, and imported owner aliases stay clean | One Chaski finding across 1,533 frontend files; stable needs second real drift and hook-tuple clean controls                       | Missing parser services leave imported owner aliases out of scope; single-slot or coordinate tuples stay clean.                                                                                                    |
| `antidrift/no-raw-fetch-in-component`             | JSX-return lexical frame plus scope-bound global fetch | JSX-returning component lexical frame to unshadowed global fetch, globalThis.fetch, window.fetch, or self.fetch calls. | Default-off inventory: report scope-bound raw fetch calls inside a JSX-returning component frame or nested callbacks/helpers declared inside that frame; module-scope helpers and local/imported fetch lookalikes stay clean. | Ready, but not stable after the June 17 narrowing; re-inventory Chaski and Codebase Atlas drift plus Chaski, Sudocode, and Murderbox clean controls before promotion | This rule emits no semantic facts; diagnostics do not depend on an optional fact sink. Module-scope helpers, hook/query abstractions, and local/imported fetch lookalikes stay clean, so the rule does not force wrapping already-owned data access. |

## Research, Delegation, And Exploration

These tracks should not block source code until they prove deterministic value.

| Track                                                                | Carrier                                                                        | Entry condition                                                                                                                                                                                                 | Drop or delegation condition                                                                                                                                           |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `antidrift/no-same-schema-recertification`                           | TypeChecker schema-output comparison                                           | Second real repo proves same-schema object-spread reparse is not a legitimate cross-source merge or boundary transform                                                                                          | Drop if Codebase Atlas remains the only ambiguous anchor.                                                                                                              |
| Zod schema forks                                                     | TypeChecker output-type and schema-owner comparison                            | Real schema object fork plus clean projection and transform controls                                                                                                                                            | Delegate or drop if ecosystem schema tooling covers the scoped behavior.                                                                                               |
| `react/no-use-state-waterfall` and `react/no-effect-fetch-waterfall` | React state/effect/fetch semantic graph                                        | Only after coupled-state and raw-fetch rules leave a proven residual gap                                                                                                                                        | Drop if existing React Hooks, raw-fetch, and coupled-state coverage own the real failures.                                                                             |
| Domain literal owners beyond status                                  | Authority index plus AST context                                               | Registry-declared literal owners and one real drift/clean pair                                                                                                                                                  | Do not become a generic magic-string rule.                                                                                                                             |
| LoRA adapters, embeddings, or code indexes                           | Offline discovery and clustering                                               | Measured candidate precision or recall improvement on real corpus owner discovery, drift clustering, or review triage                                                                                           | Never required for enforcement until deterministic proof and reproducibility exist.                                                                                    |
| C++ or Node-API semantic runtime                                     | Performance-oriented implementation detail                                     | TypeScript/ESLint fact extraction becomes a measured bottleneck and native storage accelerates joins without changing rule truth                                                                                | Do not build to replace TypeScript checker truth. Native code is not a proof carrier by itself.                                                                        |
| Change-relative and repo-graph proof                                 | Git diff, AST diff, module graph, owner index, policy script                   | The rule depends on before/after or whole-repo facts: net-new owner bias, feature scatter, high-touch growth, dependency graph changes, or diff scope.                                                          | Do not collapse into hooks. Keep inventory/advisory until the diff or graph spine has real drift, clean controls, and replayable output.                               |
| Agent-ops policy                                                     | Hooks, transcript replay, command fingerprints, runtime artifacts, PR adapters | The rule depends on session, runtime, or tool-lifecycle facts code semantics cannot recover: task intent, command freshness, tool precedence, generated artifact edits, skipped verification, or runtime proof. | Keep out of ESLint when the proof is session/runtime history rather than source semantics; do not use agent-ops as the fallback home for semantic or diff/graph proof. |

## Validation Requirements By Carrier

| Carrier                        | Required proof before promotion                                                                                                                                                        |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Plain AST or class-string rule | RuleTester coverage, real drift file, real clean control, ecosystem comparison, and documented false-positive boundary.                                                                |
| Type-aware ESLint rule         | Parser-services failure behavior, RuleTester coverage with project service, real type-aware drift and clean controls, and a non-type-aware story if the rule can run without services. |
| Semantic adapter               | Adapter unit tests, rule integration tests, fact emission where useful, no-sink behavior, invalidation cases, and real corpus classification.                                          |
| Authority registry             | Registry validation, owner-file clean controls, non-owner drift, missing-registry behavior, generated config or typed config smoke, and no unaccepted dependency-wide blocking.        |
| Static graph/config policy     | Structured input validation, graph or config evidence, replayable command output, and protected-path clean controls.                                                                   |
| Change-relative proof          | Merge-base selection, changed-file evidence, replayable command output, real drift and clean controls, and no reliance on hook-only state for proof.                                   |
| Agent-ops                      | Transcript or hook replay tests, timestamp or command-fingerprint freshness, adapter coverage for Codex, Claude, OpenCode, and PR surfaces where claimed.                              |
| Model-assisted exploration     | Offline evaluation against real corpus, precision or recall report, deterministic downstream review path, and no blocking dependency.                                                  |

## Success Criteria

Code structure is successful when rules share adapters instead of each embedding private semantic recovery, public config types expose the required settings, and package consumers can enable the rules without reaching into repo internals.

Semantic fact structure is successful when every fact kind emitted by a rule is declared in `policy/registries/rules.yaml` with its owning rules, adapter ID, carrier, confidence, emission mode, semantic association, no-sink behavior, and payload fields. Fact kinds are part of the package contract, not throwaway debugging output.

Data accuracy is successful when each blocking branch has real-corpus drift, real clean controls, no-sink or missing-config behavior, parser-service behavior where relevant, and an ecosystem comparison that explains why custom code remains necessary.

Completion is successful when every active rule has a registry row, matrix row, RuleTester coverage, corpus evidence, documented severity, and a package verification path. Rules that cannot meet the semantic proof bar must be default-off, inventory-only, ecosystem-delegated, agent-ops-only, research, or retired.
